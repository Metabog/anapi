{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Map</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Prettyname</th>
                <td>{{ entity.prettyName }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ entity.slug }}</td>
            </tr>
            <tr>
                <th>Mapdata</th>
                <td>{{ entity.mapData }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('map') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('map_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
